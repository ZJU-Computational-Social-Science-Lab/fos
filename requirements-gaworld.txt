# GAWorld runtime dependencies.
# Installed alongside FOS so the GAWorld subprocess (shared Python) can
# import them.  Kept separate from the main requirements.txt so the core
# FOS dependency list stays focused.

pandas>=2.0,<3.0
numpy>=1.24,<3.0
requests>=2.28,<3.0
matplotlib>=3.6,<4.0
networkx>=3.0,<4.0
