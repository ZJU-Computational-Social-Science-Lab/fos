[pytest]
testpaths = tests
pythonpath = src . tests/llm_prompt_testing
asyncio_mode = auto
markers =
    integration: tests that exercise integration behavior across components.
    real_llm: tests that call a real language model service.
