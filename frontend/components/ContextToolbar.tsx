/**
 * This file keeps the old workspace toolbar name available.
 *
 * ContextToolbar does not show anything because the workspace now keeps
 * run controls in the log panel instead of a separate left toolbar.
 */

const ContextToolbar = () => null;

export default ContextToolbar;
